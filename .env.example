# Hardcover API Configuration
HARDCOVER_API_TOKEN=your_hardcover_api_token_here

# Server Configuration
PORT=8080
CACHE_TTL_MINUTES=30

# CORS Configuration (required for embedding)
# For security, CORS is disabled by default. You must explicitly set allowed origins.
# Examples:
#   Single origin: ALLOWED_ORIGINS=https://mywebsite.com
#   Multiple origins: ALLOWED_ORIGINS=https://mywebsite.com,https://app.mywebsite.com
#   All origins (NOT recommended for production): ALLOWED_ORIGINS=*
ALLOWED_ORIGINS=https://yourdomain.com